import { style } from '@vanilla-extract/css';

export const funnelCardContainer = style({
  width: '100vw',
  height: '100vh',
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
});
